# Transcription mode: "local" (default, no API key needed) or "cloud"
WHISPER_MODE=local

# Local whisper model: tiny, base (default), small
# Auto-downloaded on first use (~40-250MB depending on model)
WHISPER_LOCAL_MODEL=base

# Only needed if you switch to cloud mode:
# OPENAI_API_KEY=sk-...
